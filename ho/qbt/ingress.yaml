apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: security-headers
  namespace: qbt
spec:
  headers:
    stsIncludeSubdomains: true
    stsPreload: true
    stsSeconds: 63072000
    customResponseHeaders:
      server: ""
    frameDeny: true
    customFrameOptionsValue: "SAMEORIGIN"
    contentTypeNosniff: true
    referrerPolicy: "strict-origin-when-cross-origin"
    permissionsPolicy: "camera=(), microphone=(), geolocation=()"

---

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress-plain
  namespace: qbt
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`qbt.ho.hyvel.eu`)
      middlewares:
        - name: traefik-https-redirect
          namespace: traefik
      kind: Rule
      services:
        - name: noop@internal
          kind: TraefikService

---

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress
  namespace: qbt
spec:
  tls:
    secretName: wildcard-ho-hyvel-certificate-secret
    domains:
      - main: qbt.ho.hyvel.eu
  entryPoints:
    - websecure
  routes:
    - match: Host(`qbt.ho.hyvel.eu`)
      middlewares:
        - name: security-headers
          namespace: qbt
        - name: authelia
          namespace: traefik
        - name: compress
          namespace: traefik
      kind: Rule
      services:
        - kind: Service
          name: app
          port: 7530
